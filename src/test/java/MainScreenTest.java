import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import seng201.team48.MainGameManager;
import seng201.team48.gui.MainScreenController;

import static org.junit.jupiter.api.Assertions.assertEquals;

public class MainScreenTest {
    /*private MainScreenController mainScreenController;
    private MainGameManager mainGameManager;

    @BeforeEach
    public void init(){
        mainGameManager = new MainGameManager();
        mainScreenController = new MainScreenController(mainGameManager);
    }
    @Test
    public void onOneClickedTest(){
        mainGameManager.setReload1Temp(1);
        mainScreenController.onOneClicked();
        System.out.println(mainGameManager.getReload1Temp());
        assertEquals(0, mainGameManager.getReload1Temp());
    }*/
}
